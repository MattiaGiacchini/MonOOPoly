package monoopoly;

public interface GameEngine {
	
	//Table createTable();  (I need Daniele to create Table int)
	
	//PlayerManager createPlayers(final int number); (I need Mattia to create PlayerManager)
	
	//PlayerManager currentPlayer();
	
	//List<PlayerManager> playerList();
}
